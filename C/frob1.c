#include <stdio.h>

int intScan() {
    int number;
    
    scanf("%d", &number);
    return number;
}

void arrayScan(int array[], int len) {
    for ( int i = 0; i < len; i++ ) {
        scanf("%d", &array[size]);
    }
}


int main() {
    int len = intScan();
    int array[len]
    
    return 0;
}
