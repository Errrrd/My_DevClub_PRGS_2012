// void bubbleSort(int array[], int size) {
    // int tmp;
    
    // for ( int i = 0; i < size; i++ ) {
        // for ( int j = 1, k = 0; j < size - i; j++, k++ ) {
            // if ( array[k] > array[j] ) {
                // tmp = array[j];
                // array[j] = array[k];
                // array[k] = tmp;
            // }
        // }
    // }
// }
